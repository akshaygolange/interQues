## Client Side Rendering Demo

This component fetches products from the DummyJSON API using `useEffect()` and stores them in state with `useState()`. The products are rendered in the browser after the component mounts, which makes this a Client-Side Rendering (CSR) example.